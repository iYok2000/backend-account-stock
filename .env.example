APP_ENV=development
PORT=8080

# JWT (dev only — change in production)
JWT_SECRET=dev-secret-change-in-production
JWT_ISSUER=account-stock-be
JWT_AUDIENCE=account-stock-fe

# Root provisioning (local bootstrap; rotate/remove in prod)
ROOT_EMAIL=superadmin
ROOT_PASSWORD=pass@1congrate
ROOT_CONFIRM_CODE=YIM2021

# Database (Supabase - Production)
# Example: pooled IPv4 endpoint; replace <PASSWORD> and <PROJECT_REF> as needed
# SUPABASE_DB_URL=postgresql://postgres.<PROJECT_REF>:<PASSWORD>@aws-0-ap-southeast-1.pooler.supabase.com:6543/postgres

# Database (local Postgres - Development)
# DATABASE_URL=postgres://postgres:postgres@localhost:5432/account_stock?sslmode=disable

# CORS
CORS_ORIGIN=http://localhost:3000,http://127.0.0.1:3000
